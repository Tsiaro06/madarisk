# =============================================================================
# MadaRisk Map — Frontend MODE DÉMONSTRATION
# Copier vers .env.demo. Le proxy Vite cible le backend démo (port 5001).
# =============================================================================

VITE_API_URL=/api/v1
VITE_API_PROXY_TARGET=http://localhost:5001
VITE_APP_NAME=MadaRisk Map — DÉMONSTRATION
VITE_DEMO_MODE=true
